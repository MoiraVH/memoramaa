import 'package:flutter/material.dart';

//Plain old dart object
//P O D O

class Detalles{
  String ? name;
  Color ? primary;
  Color ? secondary;
  Widget ? goto;

  Detalles(this.name,
      this.primary,
      this.secondary,
      this.goto);

}